# Word Delete MRE

A minimal example of how word deletion behaves with RTL and LTR in Bevy UI.

Run with `cargo run` and press CTRL+Backspace (or Option+Backspace on macOS) to delete the word before the cursor (or after on RTL?).
